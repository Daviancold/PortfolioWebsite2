apiVersion: v1
kind: Service
metadata:
  name: minecraft-server
  namespace: apps
  labels:
    app: minecraft-server
spec:
  type: LoadBalancer
  ports:
    - name: minecraft
      port: 25565
      targetPort: minecraft  # references the named port
      nodePort: 31234
      protocol: TCP
  selector:
    app: minecraft-server